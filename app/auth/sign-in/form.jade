div.row
  div.col-md-6.col-md-offset-3
    form(role='form')
      legend Login
      p.alert.hidden
      fieldset.col-xs-10.col-xs-offset-1
        div.form-group
          label(for='username') Username
          input.form-control(id='username', name='username', type='text',
            placeholder='Your username', required)
        div.form-group
          label(for='password') Password
          input.form-control(id='password', name='password', type='password',
            placeholder='Your password', required)
        button.btn.btn-default.pull-right(type='submit') Authenticate