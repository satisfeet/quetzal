- product = product || {};
- product.address = product.address || {};

form(role='form')
  legend Product
  p.alert.alert-danger.hidden
  fieldset
    div.form-group
      label.control-label(for='name') Name
      input.form-control(id='name', name='name', type='text',
        value=product.name, placeholder='Max Mustermann', required)
    button.btn.btn-default.pull-right(type='submit') Save